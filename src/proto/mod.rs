/// Protocol buffer definitions for HIVEFUZZ wire format.
///
/// TODO: Define .proto files and generate Rust code via prost-build.
/// For now, we use serde + bincode for serialization.
/// Protobuf will be introduced when the gossip protocol matures.
